/*
this a shorter of of declaring this. its mostly used while using html where you just use back ticks for it to work
*/

let num1 = 45,
  num2 = 30,
  result = num1 + num2;

//   this is how well use the template  literals

console.log(`the result of adding ${num1} and ${num2} is ${result}`);
//its advantages are like this 
console.log(`hello the
    im brian`); // this will print on two lines hence the difference
